# AGENTS.MD — Инструкция для AI-агентов

Этот документ является **единым источником правды** для любого AI-агента, работающего над проектом "Support admin". Он определяет рабочий процесс, правила и соглашения.

---

## 1. Базовые принципы (Core Principles)

1.  **Documentation-First Development** — *каждая* сессия начинается с чтения всех файлов в папке `docs/` (`product.md`, `tech.md`). Не начинай писать код, пока не поймешь текущую архитектуру и задачу.
2.  **Instruction-First** — Для простых задач следуй инструкциям человека напрямую. Для сложных задач — сначала планируй.
4. **Масштабируемость** - Есть значительный шанс, что проект станет полноценным. Оставляй пространство для замены решения на более подходящие для продуктива (например, если используется `SQLite`, должно быть легко перейти на `PostgreSQL`)
5. **AICODE System** — Оставляй "хлебные крошки" в коде для будущих сессий (см. раздел 4).

---

## 2. Протокол выполнения задач

### Шаг 1: Погружение в контекст
В начале любой задачи выполни команду чтения документации:
```bash
cat docs/product.md docs/tech.md
```
Это обязательно, чтобы понимать контекст проекта и не предлагать решения, противоречащие стеку (например, PostgreSQL вместо SQLite).

### Шаг 2: Оценка сложности
*   **Простая задача** (фикс бага, мелкая правка): Сразу приступай к реализации.
*   **Сложная задача** (новая фича, изменение схемы БД): Требует режима планирования (Plan Mode).

### 2.1 Определение сложности
Задача считается сложной, если затрагивает:
*   Множество модулей (например, handlers + database + services).
*   Новые интеграции (внешние API).
*   Изменение схемы БД (миграции).
*   Архитектурные изменения.

Если есть сомнения — спроси у человека. Если у человека сомнения - считай задачу сложной.

---

## 3. Режим планирования (Plan Mode)

Для сложных задач используется папка `plans/`.

1.  Создай файл плана с именем в формате: `plans/NNN-objective.md` (где NNN — инкрементальный номер, например `001-initial-setup.md`).
2.  Файл должен содержать:
    *   **Цель (Objective):** Что нужно сделать (дословно из запроса).
    *   **Дата:** Когда план был подгтовлен/изменен
    *   **Статус:** План подготовлен, утвержден, в работе, выполнен (полностью или частично), отменен.
    *   **Шаги (Proposed Steps):** Нумерованный список конкретных действий.
    *   **Риски:** Возможные проблемы.
    *   **Стратегия отката:** Что делать, если все пойдет не так.
3. Файл с планом не должен быть перегружен. Не засоряй его блоками кода. Код будет писать другой агент.
4. Пиши план так, чтобы другой агент мог понимать, какие пункты еще в работе (- [ ]), а какие уже выполнены (- [x])

**Правило:** Агент **не начинает** реализацию сложной задачи, пока человек не утвердит план (словом "Ок", "Принято" или 👍).
**Правило:** Агент **не начинает** реализацию плана, пока его статус подготовлен или отменен.

### 3.1 Работа с утвержденным планом

**ВАЖНО:** Когда план утвержден и человек просит выполнить конкретный этап (например, "этап 3.1"), делай **ТОЛЬКО** то, что указано в этом этапе.

**Правила:**
*   **Не беги вперед:** Не выполняй следующие этапы (3.2, 3.3 и т.д.), даже если они кажутся логически связанными или простыми.
*   **Спрашивай разрешения:** Если считаешь, что стоит сделать больше (например, выполнить весь раздел целиком), сначала **спроси** у человека.
*   **Обновляй план:** После завершения этапа обнови чекбоксы в плане, отметив только то, что реально выполнено.

**Исключение:** Если этап явно невозможно выполнить без другого этапа (строгая техническая зависимость), сообщи об этом человеку и попроси разрешения.

**Пример:**
*   ✅ Запрос: "выполни этап 3.1" → Делаешь только 3.1
*   ❌ Запрос: "выполни этап 3.1" → Делаешь 3.1, 3.2, 3.3 (слишком проактивно)
*   ✅ Запрос: "выполни этап 3.1" → Понимаешь, что 3.1 невозможен без 2.5 → Сообщаешь и просишь разрешения

---

## 4. Система памяти AICODE

Используй специальные комментарии-анкоры, чтобы передавать контекст следующему агенту или сессии.

Используй формат: `# AICODE-<ТИП>: <сообщение>`

*   `AICODE-NOTE:` — важное примечание о том, почему сделано именно так (например, "хак" для совместимости).
*   `AICODE-TODO:` — известная проблема или недоделка, которую надо решить в будущем, но не сейчас.
*   `AICODE-QUESTION:` — неопределенность, требующая внимания человека.

**Пример:**
```python
# AICODE-NOTE: Используем polling вместо webhook для простоты MVP
# (Пример для бота, для веб-приложения будет запуск сервера)
# await dp.start_polling(bot)

# AICODE-TODO: Добавить middleware для логирования ошибок
```

---

## 5. Технические правила (Vibe-Coding Style)

1.  **Стек:** Python 3.11+, Web Framework (TBD), SQLAlchemy, SQLite.
2.  **Асинхронность:** Весь I/O (база данных, HTTP-запросы) должен быть асинхронным (`async`/`await`).
3.  **Структура:** Строго следуй структуре папок, описанной в `docs/tech.md`. Не создавай файлы в корне, если им место в `handlers/` или `services/`.
4.  **Безопасность:** Токены только в `.env`. Никогда не хардкодь секреты.
5.  **Локальное окружение:**
    *   Перед началом работы проверь наличие файла `.agent_context.md`.
    *   Если файла нет — создай его, определи OS/Shell и запиши туда (и добавь в `.gitignore`).
    *   Используй синтаксис команд из этого файла (например, CommandSeparator).


---

## 6. Работа с системой контроля версий (Simple Vibe-Coding Git Flow)

Мы используем упрощенный подход к Git, так как работаем в связке "Человек + Агенты" в локальном репозитории.

### ❌ Анти-паттерны (Что делать НЕЛЬЗЯ):
1. **Запрещены `git diff`, `git log`, `git show`, `git blame`** для "разведки", если тебя об этом прямо не просили. Это создает информационный шум.
2. **Запрещены многострочные коммиты.** Только заголовок.
3. **Запрещен `git push`** без прямой команды.

### ✅ Правильный алгоритм коммита:
Когда задача или её логическая часть выполнена:

1. **Проверь статус:**
   ```bash
   git status
   ```
2. **Проанализируй вывод:**
   * Если в списке modified/untracked *только* те файлы, которые ты менял (или создавал) в этой сессии → **ОК**.
   * Если есть "чужие" файлы (которые ты не трогал) → **СТОП**. Спроси у человека, что делать. Предложи закоммитить только свои файлы (`git add path/to/file`).

3. **Спроси разрешение:**
   * Перед коммитом всегда спрашивай у человека: "Устраивают ли изменения?". Покажи список файлов или краткое описание.
   * **Только после аппрува** выполняй команду коммита.

4. **Закоммить:**
   ```bash
   git add .
   git commit -m "type: short description"
   git push
   ```
   * Используй английский язык.
   * *Типы:* `feat`, `fix`, `refactor`, `docs`, `chore`.
   * *Пример:* `git commit -m "fix: update status calculation logic"`

### ⚠️ Конфликты
Если `git status` или попытка коммита сообщает о конфликтах — **не пытайся решить их самостоятельно**. Остановись и сообщи человеку.

---

## 7. Самопроверка перед завершением

Перед тем как сказать "Готово", проверь себя:
1.  [ ] Код соответствует задаче?
2.  [ ] Если был план — выполнены ли запланированные на этот этап шаги?
3.  [ ] Изменения закоммичены? (git status clean)
4.  [ ] Документация (`docs/`) обновлена, если что-то изменилось?
5.  [ ] Линтеры не нужны, но код должен выглядеть опрятно (PEP8).
